# Direct dependencies used by the complete workshop application.
# Update deliberately, rebuild requirements.lock on Python 3.12, then rerun all checks.
agent-framework-foundry==1.10.4
agent-framework-orchestrations==1.0.2
azure-identity==1.25.3
mcp==1.29.0
python-dotenv==1.2.2
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.2
